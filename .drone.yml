kind: pipeline
type: kubernetes
name: k8s-pipeline-test

steps:
  - name: hello
    image: alpine:3
    commands:
      - echo "🚀 Hello from Kubernetes Drone Runner!"
      - cat /etc/os-release

  - name: kubectl-version
    image: bitnami/kubectl:latest
    commands:
      - echo "🔍 Checking cluster info..."
      - kubectl version --client
